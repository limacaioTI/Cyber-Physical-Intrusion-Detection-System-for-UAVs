# Só análise e Jupyter (sem TensorFlow — instalação bem mais rápida).
# Depois: pip install -r src/requirements.txt para o stack completo com TF.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
scipy>=1.10
jupyter>=1.0
ipykernel>=6.0
